{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ \"$POP_PERSON_PROCESS_ROLE\" = \"worker\" ]; then node --enable-source-maps artifacts/api-server/dist/worker.mjs; else node --enable-source-maps artifacts/api-server/dist/index.mjs; fi'",
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}